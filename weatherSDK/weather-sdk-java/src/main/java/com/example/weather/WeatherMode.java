package com.example.weather;

/**
 * The SDK can operate in two modes:
 * ON_DEMAND (fetch data only when requested),
 * POLLING (update data in the background on a schedule).
 */
public enum WeatherMode {
    ON_DEMAND,
    POLLING
}

